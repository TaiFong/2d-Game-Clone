{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node",
            "request": "attach",
            "program":"${workspaceFolder}/2D-GAME-CLONE",
            "args":[],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}